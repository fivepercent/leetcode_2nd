public class Solution {
    public int removeElement(int[] nums, int val) {
        int len=nums.length;
        int pointer=0;
        for(int i=0; i<len; i++){
            if(nums[i]==val){
                continue;
            }
            nums[pointer]=nums[i];
            pointer++;
        }
        return pointer;
    }
}
